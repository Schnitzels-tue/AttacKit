# Usage
To use the poisoners_kit download one of the precompiled binaries and run the executable. 

## Building from source
Building from source can be done natively or be cross compiled using the the dockerfiles in the root. execuuting the bash scripts will compile to linux amd64 and arm64.
